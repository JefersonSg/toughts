<div class="home-header">
  <h1>Todos os pensamentos</h1>
  <form class="busca" action="/" method="get">
    <input type="text" name="searsh" id="searsh" placeholder="Está buscanco por algo?">
    <button type="submit" class="btn">Buscar</button>
  </form>
  <div class="order-container">
    <span>Ordenar por:</span>
    <form action="/" method="get">
      <input type="hidden" name="searsh" value="{{this.searsh}}">
      <input type="hidden" name="order" value="new">
      <button type="submit"><i class="bi bi-arrow-up"></i></button>
    </form>
    <form action="/" method="get">
      <input type="hidden" name="searsh" value="{{this.searsh}}">
      <input type="hidden" name="order" value="old">
      <button type="submit"><i class="bi bi-arrow-down"></i></button>
    </form>
    <a href="/">Limpar</a>
  </div>
</div>
<div class="toughts-container">
  {{#each toughts}}
    <figure>
      <blockquote>
        "{{this.title}}"
      </blockquote>
      <figcaption>por <span>{{this.User.name}}</span></figcaption>
    </figure>
  {{/each}}
</div>